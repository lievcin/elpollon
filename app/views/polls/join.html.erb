<h2>
  <a id="menu-toggle" href="#" class="btn btn-mini"><i class="icon-align-justify"></i></a>	
  Únete a una polla
</h2>
<p>Puedes unirte a la polla de tus amigos y competir con ellos!</p>
<p>Solo necesitas el nombre y la clave de la polla </p>

<%= form_tag join_path, :method => 'get' do %>
  <p><%= text_field_tag :poll_search, params[:poll_search], placeholder: 'Nombre de la polla' %></p>
  <p><%= text_field_tag :poll_key, params[:poll_key], placeholder: 'Clave' %></p>
  <div class="form-actions">
    <%= submit_tag "Unirme!", :class => 'btn btn-success' %>
  </div>

<% end %>
