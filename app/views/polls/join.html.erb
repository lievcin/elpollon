<div class="row">
	<div class="span8">
		<div class="main_content">
			<h2>Únete a una polla</h2>

			<p>Puedes unirte a la polla de tus amigos y competir con ellos!</p>

			<p>Solo necesitas el nombre y la clave de la polla </p>

			<%= form_tag join_path, :method => 'get' do %>
				<p>
					<%= label_tag "Nombre de la polla" %> 
					<%= text_field_tag :poll_search, params[:poll_search] %>
					<%= label_tag "Clave" %> 
					<%= password_field_tag :poll_pass, params[:poll_pass] %>
					<%= submit_tag "Unirme!", :name => nil, :class => 'btn btn-success' %>
				</p>
			<% end %>
			<ul>
			</ul>
		</div>
	</div>
</div>